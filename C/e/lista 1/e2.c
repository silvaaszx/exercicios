#include <stdio.h>

int main() {
    int e = 5; 
    if (e == 1) {
        printf("%d erro detectado.\n", e);
    } else {
        printf("%d erros detectados.\n", e);
    }

    return 0;
}
